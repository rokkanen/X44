"net-core file packages" contains files that are copied after standard processing "dotnet add" command
In order to merge files correctly, just follow directory structure above:

src
  |__Company.Template.[Layer]
	|__[Any organisation file you want]


